<UserControl x:Class="FixCars4Us.App.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FixCars4Us.App">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- KLIENCI -->
        <GroupBox Grid.Column="0" Header="Klienci">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Nowy klient" FontWeight="Bold"/>
                    <TextBox Text="{Binding NewCustomerName}" ToolTip="Nazwa / imię i nazwisko" local:Placeholder.Text="np. Jan Kowalski"/>
                    <TextBox Text="{Binding NewCustomerPhone}" ToolTip="Telefon" local:Placeholder.Text="np. 600 123 456"/>
                    <TextBox Text="{Binding NewCustomerEmail}" ToolTip="E-mail" local:Placeholder.Text="np. jan.kowalski@email.pl"/>
                    <ComboBox ItemsSource="{Binding CustomerTypes}" SelectedItem="{Binding NewCustomerType}"/>
                    <TextBox Text="{Binding NewCustomerDiscount}" ToolTip="Rabat stały %" local:Placeholder.Text="np. 5"/>
                    <Button Content="Dodaj klienta" Command="{Binding AddCustomerCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Type}"/>
                        <DataGridTextColumn Header="Rabat%" Binding="{Binding DiscountPercent}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <!-- POJAZDY -->
        <GroupBox Grid.Column="1" Header="Pojazdy klienta">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Nowy pojazd" FontWeight="Bold"/>
                    <TextBox Text="{Binding NewVehicleReg}" ToolTip="Nr rejestracyjny" local:Placeholder.Text="np. WA12345"/>
                    <TextBox Text="{Binding NewVehicleVin}" ToolTip="VIN" local:Placeholder.Text="np. WVWZZZ1JZXW000001"/>
                    <TextBox Text="{Binding NewVehicleBrand}" ToolTip="Marka" local:Placeholder.Text="np. Volkswagen"/>
                    <TextBox Text="{Binding NewVehicleModel}" ToolTip="Model" local:Placeholder.Text="np. Golf"/>
                    <TextBox Text="{Binding NewVehicleMileage}" ToolTip="Przebieg" local:Placeholder.Text="np. 120000"/>
                    <Button Content="Dodaj pojazd" Command="{Binding AddVehicleCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nr rej." Binding="{Binding RegistrationNumber}"/>
                        <DataGridTextColumn Header="Marka" Binding="{Binding Brand}"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                        <DataGridTextColumn Header="Przebieg" Binding="{Binding Mileage}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <!-- HISTORIA SERWISOWA -->
        <GroupBox Grid.Column="2" Header="Historia serwisowa pojazdu">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Nowy wpis historii" FontWeight="Bold"/>
                    <TextBox Text="{Binding NewHistoryDescription}" ToolTip="Opis" local:Placeholder.Text="np. Wymiana oleju"/>
                    <TextBox Text="{Binding NewHistoryMileage}" ToolTip="Przebieg" local:Placeholder.Text="np. 125000"/>
                    <TextBox Text="{Binding NewHistoryCost}" ToolTip="Koszt" local:Placeholder.Text="np. 250"/>
                    <Button Content="Dodaj wpis" Command="{Binding AddHistoryCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding History}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=d}"/>
                        <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Koszt" Binding="{Binding Cost}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
