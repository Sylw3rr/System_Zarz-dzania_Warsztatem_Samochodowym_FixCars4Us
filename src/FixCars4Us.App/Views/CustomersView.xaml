<UserControl x:Class="FixCars4Us.App.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:FixCars4Us.App">
    <Grid Margin="6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- KLIENCI -->
        <GroupBox Grid.Column="0" Header="Klienci">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Nowy klient" FontWeight="Bold"/>
                    <TextBlock Text="Nazwa / imię i nazwisko (np. Jan Kowalski)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewCustomerName}"/>
                    <TextBlock Text="Telefon (np. 600 123 456)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewCustomerPhone}"/>
                    <TextBlock Text="E-mail (np. jan.kowalski@email.pl)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewCustomerEmail}"/>
                    <TextBlock Text="Typ klienta" Foreground="Gray"/>
                    <ComboBox ItemsSource="{Binding CustomerTypes}" SelectedItem="{Binding NewCustomerType}"/>
                    <TextBlock Text="Rabat stały % (np. 5)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewCustomerDiscount}"/>
                    <Button Content="Dodaj klienta" Command="{Binding AddCustomerCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding Type}"/>
                        <DataGridTextColumn Header="Rabat%" Binding="{Binding DiscountPercent}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <!-- POJAZDY -->
        <GroupBox Grid.Column="1" Header="Pojazdy klienta">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock Text="Nowy pojazd" FontWeight="Bold"/>
                    <TextBlock Text="Nr rejestracyjny (np. WA12345)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewVehicleReg}"/>
                    <TextBlock Text="VIN (np. WVWZZZ1JZXW000001)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewVehicleVin}"/>
                    <TextBlock Text="Marka (np. Volkswagen)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewVehicleBrand}"/>
                    <TextBlock Text="Model (np. Golf)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewVehicleModel}"/>
                    <TextBlock Text="Przebieg km (np. 120000)" Foreground="Gray"/>
                    <TextBox Text="{Binding NewVehicleMileage}"/>
                    <Button Content="Dodaj pojazd" Command="{Binding AddVehicleCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nr rej." Binding="{Binding RegistrationNumber}"/>
                        <DataGridTextColumn Header="Marka" Binding="{Binding Brand}"/>
                        <DataGridTextColumn Header="Model" Binding="{Binding Model}"/>
                        <DataGridTextColumn Header="Przebieg" Binding="{Binding Mileage}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>

        <!-- HISTORIA SERWISOWA -->
        <GroupBox Grid.Column="2" Header="Historia serwisowa pojazdu">
            <DockPanel>
                <DataGrid ItemsSource="{Binding History}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=d}"/>
                        <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="Koszt" Binding="{Binding Cost}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </GroupBox>
    </Grid>
</UserControl>
